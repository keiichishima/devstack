# lib/cinder_backends/ukai
# Configure the ukai backend

# Enable with:
#
#   CINDER_ENABLED_BACKENDS+=,ukai:<TYPENAME>

# Save trace setting
UKAI_XTRACE=$(set +o | grep xtrace)
set +o xtrace


# Entry Points
# ------------

# configure_cinder_backend_ukai - Set config files, create data dirs, etc
function configure_cinder_backend_ukaiblock {
    local be_name=$1
    iniset $CINDER_CONF $be_name volume_backend_name $be_name
    iniset $CINDER_CONF $be_name volume_driver "cinder.volume.drivers.ukaiblock.UkaiDriver"
}

# Restore xtrace
$UKAI_XTRACE

# Local variables:
# mode: shell-script
# End:
